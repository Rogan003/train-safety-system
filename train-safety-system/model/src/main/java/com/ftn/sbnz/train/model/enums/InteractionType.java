package com.ftn.sbnz.train.model.enums;

public enum InteractionType { // are all these necessary? or should I refactor to just use brake apply?
    SIFA_RESET,
    BRAKE_APPLY,
    BRAKE_RELEASE,
    THROTTLE_UP,
    THROTTLE_DOWN
}
